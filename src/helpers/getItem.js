
export const getItem = (item) => {

    return new Promise((resolve, reject) => {
        if (true) {
            resolve(item)
        }
        else {
            reject('No funciona el itemDetailContainer')
        }
    })

}
